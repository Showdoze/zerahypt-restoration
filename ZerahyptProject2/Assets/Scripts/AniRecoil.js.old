var Force : float = 100;
var DoingRecoil : boolean = false;

function Update () {
			if(DoingRecoil == true){
         GetComponent.<Rigidbody>().AddForce(transform.up * Force);
         DoingRecoil = false;
}
}